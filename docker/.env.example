# =============================================================================
# RescueStream Backend Services Configuration
# =============================================================================
# Copy this file to docker/.env and fill in your values.
# This file is used by docker-compose.yml for backend service configuration.
# Generate secrets with: openssl rand -hex 32
# =============================================================================

# -----------------------------------------------------------------------------
# Database (PostgreSQL)
# -----------------------------------------------------------------------------
# Database username (default: postgres)
POSTGRES_USER=postgres
# Database password - CHANGE IN PRODUCTION!
POSTGRES_PASSWORD=postgres
# Database name
POSTGRES_DB=rescuestream
# Host port for PostgreSQL (change if 5432 is in use)
POSTGRES_PORT=5432

# -----------------------------------------------------------------------------
# Backend API
# -----------------------------------------------------------------------------
# API server port
API_PORT=8080
# HMAC authentication secret - CHANGE IN PRODUCTION!
# Generate with: openssl rand -hex 32
API_SECRET=dev-secret-change-in-production

# -----------------------------------------------------------------------------
# MediaMTX (Streaming Server)
# -----------------------------------------------------------------------------
# WebRTC ICE public IP for NAT traversal
# Set to your machine's local IP for LAN access, or public IP for external access
# Find your local IP: ifconfig | grep "inet " | grep -v 127.0.0.1
MTX_WEBRTCICEHOSTNAT1TO1IPS=localhost
