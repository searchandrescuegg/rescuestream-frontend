# =============================================================================
# RescueStream Frontend Configuration (Next.js)
# =============================================================================
# Copy this file to .env and fill in your values.
# Required variables are marked with [REQUIRED].
# Generate secrets with: openssl rand -base64 32
#
# For backend service configuration, see: docker/.env.example
# =============================================================================

# Auth.js Configuration
# [REQUIRED] Session encryption key - generate with: openssl rand -base64 32
AUTH_SECRET="generate-with-openssl-rand-base64-32"
# [REQUIRED] Google OAuth client ID - get from https://console.cloud.google.com/apis/credentials
AUTH_GOOGLE_ID="your-google-client-id"
# [REQUIRED] Google OAuth client secret - get from Google Cloud Console
AUTH_GOOGLE_SECRET="your-google-client-secret"

# Auth Allowlist Configuration
# Controls which users can access the RescueStream dashboard
# At least one of these must be configured, or all access is denied (fail-closed security)
#
# Comma-separated list of allowed email domains (e.g., "rescue.stream,partner.org")
# Users from these domains are authorized to access the dashboard
# - Case-insensitive matching (Rescue.Stream matches rescue.stream)
# - Subdomains require explicit entries (sub.company.com won't match company.com)
# - Leading @ is optional (@company.com and company.com are equivalent)
AUTH_ALLOWED_DOMAINS="rescue.stream"

# Comma-separated list of specific allowed email addresses
# These users are authorized regardless of their domain (for external collaborators)
# - Case-insensitive matching
# - Checked before domain allowlist (more specific match takes precedence)
AUTH_ALLOWED_EMAILS=""

# RTMP Ingest Configuration
# URL displayed to users for streaming (without trailing slash)
# This is fetched at runtime, so changes take effect without rebuilding
RTMP_INGEST_URL="rtmp://localhost:1935"

# RescueStream API Connection
# [REQUIRED] URL for the backend API (use http://localhost:8080 for local development)
RESCUESTREAM_API_URL="http://localhost:8080"
# [REQUIRED] API key for HMAC authentication - must match API_KEY in backend
RESCUESTREAM_API_KEY="your-api-key"
# [REQUIRED] API secret for HMAC authentication - must match API_SECRET in backend
# Default matches docker/.env.example for local development
RESCUESTREAM_API_SECRET="dev-secret-change-in-production"
